# 05-03 Plan: Readiness Observability and Regression Coverage

## Goal

Operators can distinguish readiness scheduling decisions from upstream 429/529, account limits, and normal no-account failures.

## Tasks

1. Add structured scheduler logs for readiness status, cache hit, TTL, retry-after, safeConcurrency, requested/effective model, account ID, group/channel ID where available, and redacted provider identifier.
2. Extend OpenAI scheduler decision metadata where useful for tests and admin/ops inspection.
3. Add regression tests covering blocked, degraded, cache hit, fail-open, fail-closed, previous-response sticky, session sticky, load-balance, and fallback-wait.
4. Run focused service tests and update Phase 5 summary with exact commands.

## Acceptance

- Readiness-blocked scheduling is visible and distinct from upstream 429/529 handling.
- Logs contain no secrets.
- Focused tests pass in `/www/sub2api`.
