---
phase: 04-opus-4-7-readiness-contract-and-scheduler-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pool/account.go
  - pool/account_test.go
autonomous: true
requirements:
  - RDY-03
user_setup: []
must_haves:
  truths:
    - "Kiro-Go remembers recent real content success for each account plus normalized Opus 4.7 model key."
    - "Recent real success improves routing priority only after normal account/model eligibility filters accept a candidate."
    - "Historical success never routes to disabled, cooling-down, breaker-open, token-expired, usage-limited, or model-invisible accounts."
  artifacts:
    - path: "pool/account.go"
      provides: "Thread-safe in-memory account/model real content-success evidence and routing priority hook"
      contains: "RecordModelContentSuccess"
    - path: "pool/account_test.go"
      provides: "Pool tests for content-success priority and eligibility blockers"
      contains: "ContentSuccess"
  key_links:
    - from: "pool/account.go"
      to: "pool/breaker.go"
      via: "same normalized account/model key conventions"
      pattern: "normalize.*model|model.*key"
    - from: "pool/account.go"
      to: "pool/account_test.go"
      via: "selection tests using BeginNextForModelExcept"
      pattern: "BeginNextForModel"
---

<objective>
Add the account-pool side of real Opus 4.7 content-success learning and routing priority.

Purpose: RDY-03 requires account-level evidence to affect routing priority for otherwise eligible accounts, without persisting secret-bearing runtime config or bypassing existing blockers.
Output: Pool APIs for recording/querying recent account+model success and tests proving priority cannot override eligibility.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-SPEC.md
@.planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-CONTEXT.md
@.planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-RESEARCH.md
@.planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-PATTERNS.md
@.planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@pool/account.go
@pool/breaker.go
@pool/account_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add in-memory account/model content-success evidence</name>
  <read_first>
    - .planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-SPEC.md
    - .planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-CONTEXT.md
    - .planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-RESEARCH.md
    - pool/account.go
    - pool/breaker.go
    - pool/account_test.go
  </read_first>
  <files>pool/account.go, pool/account_test.go</files>
  <behavior>
    - Recording `accountID + claude-opus-4-7 + timestamp` stores a queryable latest success timestamp.
    - Recording an older timestamp after a newer timestamp does not move the latest timestamp backward.
    - Different accounts and different normalized model keys do not overwrite each other.
    - The state is protected by the existing `AccountPool` mutex and is not persisted to `config/config.go` or `data/config.json`.
  </behavior>
  <action>Add package-level pool support for recent real content-success evidence. In `AccountPool`, add a bounded in-memory map keyed by account ID and normalized model key. Expose `RecordModelContentSuccess(accountID, model string, at time.Time)` and `ModelContentSuccess(accountID, model string) (time.Time, bool)` methods. Use the same normalized model-key semantics used by model-list and breaker code, and ignore blank account IDs, blank models, and zero timestamps. Do not add fields to `config.Account` and do not read runtime config secrets.</action>
  <verify>
    <automated>go test ./pool -run 'Test.*ContentSuccess' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `pool/account.go` contains exported recording and read methods on `AccountPool`.
    - `pool/account_test.go` proves latest timestamp behavior, model normalization, account isolation, and no config persistence requirement.
    - `go test ./pool -run 'Test.*ContentSuccess' -count=1` passes.
  </acceptance_criteria>
  <done>Account-level real content success can be recorded and queried safely for `account + claude-opus-4.7`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bias eligible Opus 4.7 account selection by fresher success</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-SPEC.md
    - .planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-PATTERNS.md
    - pool/account.go
    - pool/breaker.go
    - pool/account_test.go
  </read_first>
  <files>pool/account.go, pool/account_test.go</files>
  <behavior>
    - With two otherwise eligible Opus 4.7 accounts, `BeginNextForModelExcept("claude-opus-4-7", nil)` selects the account with fresher real content-success evidence.
    - The same fresher-success account is skipped when disabled, cooling down, breaker-open, token-expired, usage-limited, or model-not-listed.
    - Existing model visibility and breaker filters still run before success-evidence comparison.
  </behavior>
  <action>Update the account candidate comparison path used by `getNextForModelExceptLocked`, `BeginNextForModelExcept`, and `BeginNextForModelSessionExcept` so fresher account+model content-success evidence is considered only after an account has passed existing eligibility filters. If `isBetterCandidateLocked` needs the model, change the comparator signature and all local call sites together. Preserve existing strategy behavior where no candidate has success evidence. Add tests that seed model lists and breaker/cooldown/token/usage blockers explicitly.</action>
  <verify>
    <automated>go test ./pool -run 'TestBeginNextForModel|Test.*ContentSuccess' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Pool selection prefers the fresher real-success account only for otherwise eligible Opus 4.7 candidates.
    - Disabled, cooldown, breaker, token-expiry, usage-limit, and model-list blockers each defeat historical success evidence.
    - `go test ./pool -run 'TestBeginNextForModel|Test.*ContentSuccess' -count=1` passes.
  </acceptance_criteria>
  <done>RDY-03 routing priority is implemented in the pool selection owner, not duplicated in admin API code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handler-to-pool evidence update | Later request handlers will pass account/model success evidence into pool runtime state. |
| pool-to-routing decision | Runtime state influences which upstream account receives a request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `pool.RecordModelContentSuccess` | mitigate | Ignore blank IDs/models and zero timestamps; keep latest timestamp monotonic per account/model key. |
| T-04-02 | Elevation of Privilege | `pool.getNextForModelExceptLocked` | mitigate | Apply success-evidence priority only after existing enabled, cooldown, breaker, token, usage, and model-list eligibility filters. |
| T-04-03 | Information Disclosure | `pool/account.go` runtime evidence | mitigate | Store only account ID, normalized model key, and timestamp; do not persist or expose tokens, config contents, or raw credential material. |
| T-04-SC | Tampering | package installs | accept | No npm/pip/cargo package installs are planned in this phase plan. |
</threat_model>

<verification>
Focused gate: `go test ./pool -run 'TestBeginNextForModel|Test.*ContentSuccess' -count=1`
Phase sampling gate after this plan: `go test ./pool -count=1`
</verification>

<success_criteria>
- RDY-03 pool evidence APIs exist and are tested.
- Fresher real Opus 4.7 success affects only eligible account selection.
- No runtime secret files are read or referenced.
</success_criteria>

<output>
Create `.planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-01-SUMMARY.md` when done.
</output>
