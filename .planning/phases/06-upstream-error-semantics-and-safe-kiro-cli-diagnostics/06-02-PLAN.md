# 06-02 Plan: Wire Structured Reasons into Retry, Cooldown, Breaker, and Quiet Mode

## Goal

Structured upstream reasons drive the existing cooldown, breaker, retry-after, request log, and quiet-mode behavior without broad scheduler rewrites.

## Tasks

1. Keep account-level failures scoped to the selected account and preserve `model_capacity` as model breaker pressure without account cooldown mutation.
2. Ensure `rateLimitResetFromError`, `claudeErrorHeadersForUpstreamError`, and OpenAI/Claude status/type helpers use normalized retry-after and reason evidence.
3. Preserve request log attempt `Reason` and `RetryAfterSeconds` from structured errors.
4. Add or adjust tests proving temporary-limited accounts are skipped while another account remains viable, and model capacity does not globally poison the Opus 4.7 pool.
5. Verify background refresh, model refresh, health check, and live admin test paths remain quiet-mode/cooldown aware.

## Acceptance

- Account-level and model-level effects remain distinct.
- Retry-after survives classification into safe headers and attempt traces.
- Unsafe upstream headers are not propagated.
- Existing quiet-mode tests pass, with any new structured-pressure evidence covered by focused tests.
