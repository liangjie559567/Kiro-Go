---
phase: "03"
plan: "01"
type: "implementation"
wave: 1
depends_on: []
files_modified:
  - proxy/ecosystem_ops.go
  - proxy/ecosystem_ops_test.go
  - proxy/handler.go
autonomous: true
requirements: [KE-01, KE-02]
---

# Phase 03 Plan 01: Credential Validation and Account Diagnostics

<objective>
Add dry-run credential validation and a single account diagnostics response without reading local secret files.
</objective>

<tasks>
- Add `/admin/api/auth/credentials/validate` for Kiro Account Manager-style JSON fixtures.
- Return per-account `valid`, `invalid`, or `unsupported` results without mutating config.
- Add `/admin/api/accounts/{id}/diagnostics` with auth, token, profile, model-cache, quota, proxy, cooldown, and runtime-health checks.
</tasks>

<verification>
go test ./proxy -run 'TestValidateCredentialSource|TestAccountDiagnostics' -count=1
</verification>
