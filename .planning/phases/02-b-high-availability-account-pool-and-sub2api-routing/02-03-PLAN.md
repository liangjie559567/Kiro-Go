---
phase: "02"
plan: "03"
type: "verification"
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - .planning/phases/02-b-high-availability-account-pool-and-sub2api-routing/02-VERIFICATION.md
autonomous: true
requirements:
  - HA-06
  - HA-07
must_haves:
  checks:
    - go test ./proxy ./pool -run 'Temporary|Failure|Cooldown|Health|Refresh|RequestLog|Admission|NoAvailable|sub2api|Retry|FallsThrough|RateLimit' -count=1
    - go test ./... -count=1
---

# Phase 02 Plan 03: HA Verification and sub2api Evidence Reconciliation

<objective>
Verify current-code HA behavior through automated tests and reconcile it with existing `/www/sub2api` black-box UAT evidence without claiming a fresh live PASS.
</objective>

<tasks>
- Run focused HA tests across `proxy` and `pool`.
- Run full repository tests.
- Write a Phase 2 verification report with automated status, historical UAT references, and latest-code live UAT blocker.
- Explicitly record that `/www/sub2api` source was not modified for this phase.
</tasks>

<verification>
go test ./proxy ./pool -run 'Temporary|Failure|Cooldown|Health|Refresh|RequestLog|Admission|NoAvailable|sub2api|Retry|FallsThrough|RateLimit' -count=1
go test ./... -count=1
</verification>

<success_criteria>
- Current-code automated HA behavior passes.
- Historical UAT evidence is linked for regression context.
- Final status remains `human_needed` until live 10x10 is rerun on latest code.
</success_criteria>
