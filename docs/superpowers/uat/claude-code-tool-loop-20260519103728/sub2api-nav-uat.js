const fs=require('fs'); const path=require('path'); const {chromium}=require('/root/.claude/gsd-user-files-backup/skills/gstack/node_modules/playwright');
const [outDir, pass]=process.argv.slice(2); const summary={screenshots:[],console:[],pageErrors:[],requestFailures:[]};
async function shot(page,name){const file=path.join(outDir,name); await page.screenshot({path:file,fullPage:true}); const text=await page.locator('body').innerText().catch(()=> ''); summary.screenshots.push({name,bytes:fs.statSync(file).size,url:page.url(),text:text.slice(0,5000)});}
(async()=>{const browser=await chromium.launch({headless:true,executablePath:'/usr/bin/google-chrome',args:['--no-sandbox','--disable-dev-shm-usage']}); const context=await browser.newContext({viewport:{width:1440,height:1000}}); context.on('page',p=>{p.on('console',m=>{if(['error','warning'].includes(m.type())) summary.console.push({type:m.type(),text:m.text(),url:p.url()})}); p.on('pageerror',e=>summary.pageErrors.push({message:e.message,url:p.url()})); p.on('requestfailed',r=>summary.requestFailures.push({url:r.url(),failure:r.failure()?.errorText}))}); const page=await context.newPage(); await page.goto('http://127.0.0.1:18080/login',{waitUntil:'domcontentloaded'}).catch(()=>{}); await page.waitForTimeout(1000); const email=page.locator('input[type="email"], input[name="email"], input[placeholder*="email" i]').first(); if(await email.count()) await email.fill('admin@sub2api.local').catch(()=>{}); const pwd=page.locator('input[type="password"], input[name="password"]').first(); if(await pwd.count()) await pwd.fill(pass).catch(()=>{}); await page.locator('button').filter({hasText:/login|登录|sign in/i}).first().click().catch(()=>{}); await page.waitForTimeout(2500); await shot(page,'sub2api-nav-dashboard.png');
for (const [text,file] of [['Accounts','sub2api-nav-accounts.png'],['Groups','sub2api-nav-groups.png'],['Usage','sub2api-nav-usage.png']]) { const item=page.locator('a,button,[role="button"]').filter({hasText:new RegExp('^'+text+'$','i')}).first(); if(await item.count()) { await item.click().catch(()=>{}); await page.waitForTimeout(1800); } await shot(page,file); }
fs.writeFileSync(path.join(outDir,'sub2api-nav-summary.json'),JSON.stringify(summary,null,2)); await browser.close();})().catch(e=>{summary.fatal=String(e&&e.stack||e); fs.writeFileSync(path.join(outDir,'sub2api-nav-summary.json'),JSON.stringify(summary,null,2)); process.exit(1);});
