const fs=require('fs'); const path=require('path'); const {chromium}=require('/root/.claude/gsd-user-files-backup/skills/gstack/node_modules/playwright');
const [outDir, pass]=process.argv.slice(2); const summary={screenshots:[],console:[],pageErrors:[],requestFailures:[]};
async function shot(page,name){const file=path.join(outDir,name); await page.screenshot({path:file,fullPage:true}); const text=await page.locator('body').innerText().catch(()=> ''); summary.screenshots.push({name,bytes:fs.statSync(file).size,url:page.url(),text:text.slice(0,5000)});}
(async()=>{const browser=await chromium.launch({headless:true,executablePath:'/usr/bin/google-chrome',args:['--no-sandbox','--disable-dev-shm-usage']}); const context=await browser.newContext({viewport:{width:1440,height:1000}}); const page=await context.newPage(); page.on('console',m=>{if(['error','warning'].includes(m.type())) summary.console.push({type:m.type(),text:m.text()})}); page.on('pageerror',e=>summary.pageErrors.push({message:e.message})); page.on('requestfailed',r=>summary.requestFailures.push({url:r.url(),failure:r.failure()?.errorText})); await page.goto('http://127.0.0.1:18080/login',{waitUntil:'domcontentloaded'}).catch(()=>{}); await page.waitForTimeout(1000); const email=page.locator('input[type="email"], input[name="email"], input[placeholder*="email" i]').first(); if(await email.count()) await email.fill('admin@sub2api.local').catch(()=>{}); const pwd=page.locator('input[type="password"], input[name="password"]').first(); if(await pwd.count()) await pwd.fill(pass).catch(()=>{}); await page.locator('button').filter({hasText:/login|登录|sign in/i}).first().click().catch(()=>{}); await page.waitForTimeout(2500); await shot(page,'sub2api-simple-dashboard.png'); for (const [label,file] of [['Accounts','sub2api-simple-accounts.png'],['Groups','sub2api-simple-groups.png'],['Usage','sub2api-simple-usage.png']]) { await page.getByText(label, {exact:true}).first().click({timeout:5000}).catch(()=>{}); await page.waitForTimeout(1500); await shot(page,file); } fs.writeFileSync(path.join(outDir,'sub2api-simple-nav-summary.json'),JSON.stringify(summary,null,2)); await browser.close();})().catch(e=>{summary.fatal=String(e&&e.stack||e); fs.writeFileSync(path.join(outDir,'sub2api-simple-nav-summary.json'),JSON.stringify(summary,null,2)); process.exit(1);});
