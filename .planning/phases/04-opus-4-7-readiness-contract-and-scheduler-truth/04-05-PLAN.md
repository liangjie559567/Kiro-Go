---
phase: 04-opus-4-7-readiness-contract-and-scheduler-truth
plan: 05
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - web/index.html
autonomous: true
requirements:
  - RDY-01
  - RDY-02
  - RDY-03
  - RDY-04
user_setup: []
must_haves:
  truths:
    - "The existing admin fleet readiness card renders the versioned Opus 4.7 contract fields."
    - "Operators can quickly compare status, safe concurrency, retry timing, reason codes, real content success, and stable fallback evidence."
    - "The UI renders masked account evidence only and never exposes runtime secrets."
  artifacts:
    - path: "web/index.html"
      provides: "Admin Opus 4.7 fleet health card rendering for readiness contract"
      contains: "Opus 4.7 fleet health"
  key_links:
    - from: "web/index.html"
      to: "/admin/api/fleet/readiness?model=claude-opus-4-7"
      via: "existing loadFleetReadiness fetch"
      pattern: "fleet/readiness\\?model=claude-opus-4-7"
---

<objective>
Expose the Phase 4 readiness contract in the existing admin UI fleet health card.

Purpose: RDY-01 through RDY-04 need admin-visible evidence that uses the same existing route as the API contract and preserves the compact single-file admin UI.
Output: `web/index.html` fleet readiness card rendering contract version, status, safe concurrency, retry timing, reason codes, and content/fallback evidence.
</objective>

<execution_context>
@/root/.codex/get-shit-done/workflows/execute-plan.md
@/root/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-SPEC.md
@.planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-CONTEXT.md
@.planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-RESEARCH.md
@.planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-UI-SPEC.md
@.planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-PATTERNS.md
@.planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-VALIDATION.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@web/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render readiness contract fields in the existing fleet card</name>
  <read_first>
    - .planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-UI-SPEC.md
    - .planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-SPEC.md
    - web/index.html
  </read_first>
  <files>web/index.html</files>
  <action>Update only the existing `loadFleetReadiness` rendering for the `Opus 4.7 fleet health` card. Preserve the existing refresh button and compact card style. Render labels exactly from the UI spec: `Contract`, `Status`, `Safe concurrency`, `Retry after`, `Reasons`, `Real content success`, and `Stable fallbacks`. Include locally schedulable account count, recommended action, and compact account rows when the API returns safe row fields. Long reason-code arrays must wrap inside the card. Use existing escaping helpers for all dynamic text.</action>
  <verify>
    <automated>rg -n 'Contract|Safe concurrency|Retry after|Reasons|Real content success|Stable fallbacks|Opus 4\\.7 fleet health' web/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `web/index.html` still fetches `/admin/api/fleet/readiness?model=claude-opus-4-7`.
    - The card renders the UI-spec labels exactly.
    - The layout remains a compact operational card and does not introduce a build step, design system, icon package, or large layout rewrite.
  </acceptance_criteria>
  <done>The admin UI can display the readiness contract fields produced by Plan 04-03.</done>
</task>

<task type="auto">
  <name>Task 2: Guard UI rendering against secret exposure and missing fields</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-UI-SPEC.md
    - web/index.html
  </read_first>
  <files>web/index.html</files>
  <action>Ensure the fleet card empty/error state remains `Fleet readiness unavailable`. Render only safe API fields: contract version, status, safe concurrency, locally schedulable count, retry-after, recommended action, reason codes, aggregate content/fallback counters, account ID, masked email, eligibility, reason codes, model cache, runtime health, and content-success timestamp. Do not render tokens, raw headers, CLI paths, runtime config paths, or raw account credential fields. Keep text responsive with wrapping for reason codes and account rows.</action>
  <verify>
    <automated>awk '/async function loadFleetReadiness\\(\\)/,/async function loadWebSearchDiagnostics\\(\\)/' web/index.html | rg -n 'data/config|rawHeaders|KIRO_CLI_HOME|accessToken|refreshToken|clientSecret|apiKey|credential'; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - Empty/error state text is `Fleet readiness unavailable`.
    - The changed fleet card does not reference secret-bearing field names or runtime config paths.
    - Dynamic account text uses masked email or safe account IDs only.
  </acceptance_criteria>
  <done>UI exposure stays operational, compact, and safe for admin evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin-api-to-browser | Readiness JSON is rendered in the admin browser UI. |
| runtime-state-to-operator | Account/runtime health evidence is shown to authenticated operators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Information Disclosure | `web/index.html` fleet readiness card | mitigate | Render only safe fields and masked email; grep for secret-bearing identifiers in changed UI. |
| T-04-16 | Tampering | dynamic readiness HTML | mitigate | Use existing escaping helpers for all dynamic text and wrap reason-code arrays without raw HTML injection. |
| T-04-17 | Spoofing | UI status display | mitigate | Display API-provided `status`, `reasonCodes`, content-success counters, and stable fallback counters separately. |
| T-04-SC | Tampering | package installs | accept | No npm/pip/cargo package installs are planned in this phase plan. |
</threat_model>

<verification>
Focused gate: `rg -n 'Contract|Safe concurrency|Retry after|Reasons|Real content success|Stable fallbacks|Opus 4\.7 fleet health' web/index.html`
Backend compatibility gate: `go test ./proxy -run TestFleetReadiness -count=1`
</verification>

<success_criteria>
- Existing admin fleet card displays the new readiness contract safely.
- UI labels match the approved Phase 04 UI spec.
- No runtime secret names or raw credential fields are rendered.
</success_criteria>

<output>
Create `.planning/phases/04-opus-4-7-readiness-contract-and-scheduler-truth/04-05-SUMMARY.md` when done.
</output>
